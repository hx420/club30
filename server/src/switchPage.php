<?php require_once 'conn.php'; require_once './statusCode.php'; $statusCode = $_POST['statusCode']; if (isset($statusCode) && isValidStatusCode($statusCode)){ $endpoint = "SELECT * FROM switch_data";  $result = $conn->query($endpoint); if ($conn->error) { die("FALSE：" . $conn->error); } $switch = $result -> fetch_all(MYSQLI_ASSOC); header("Content-Type: application/json; charset=utf-8");  echo json_encode([ "errno" => 0,  "switch" => $switch, ]); $conn->close();   }else{header('Content-Type: application/json'); echo json_encode([ 'errno' => 1, 'message' => '无权访问！！！' ]);} ?>  